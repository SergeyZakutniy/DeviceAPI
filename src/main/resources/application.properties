spring.application.name=deviceapi

# Postgre DB
# These props can be extracted to env variables
spring.datasource.url=jdbc:postgresql://localhost:5436/deviceapidb
spring.datasource.username=deviceapidbuser
spring.datasource.password=SuperStr0ngPaSSw0rd
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#spring.threads.virtual.enabled=true

#management.endpoints.web.exposure.include=health,info,prometheus,metrics,env,configprops
# Should be enabled only for development purposes
#management.endpoint.env.show-values=always
#management.endpoint.configprops.show-values=always
